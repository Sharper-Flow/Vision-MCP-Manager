<svg width="1280" height="640" viewBox="0 0 1280 640" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <!-- Blur filters - 5x blurrier -->
    <filter id="blur-glow" x="-100%" y="-100%" width="300%" height="300%">
      <feGaussianBlur in="SourceGraphic" stdDeviation="40" result="blur1"/>
    </filter>
    <filter id="blur-medium" x="-100%" y="-100%" width="300%" height="300%">
      <feGaussianBlur in="SourceGraphic" stdDeviation="20" result="blur2"/>
    </filter>
    <filter id="blur-subtle" x="-100%" y="-100%" width="300%" height="300%">
      <feGaussianBlur in="SourceGraphic" stdDeviation="10" result="blur3"/>
    </filter>
    
    <!-- Geist Mono approximation using system monospace -->
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Geist+Mono:wght@400;500&amp;display=swap');
      .vision-text {
        font-family: 'Geist Mono', 'SF Mono', 'Fira Code', 'Consolas', monospace;
        font-weight: 500;
        letter-spacing: 0.15em;
      }
    </style>
  </defs>
  
  <!-- Black background -->
  <rect width="100%" height="100%" fill="#000000"/>
  
  <!-- Outermost blur layer (most diffuse glow) -->
  <text 
    x="640" 
    y="340" 
    class="vision-text"
    font-size="140"
    fill="rgba(255,255,255,0.2)"
    text-anchor="middle"
    filter="url(#blur-glow)">
    Vision
  </text>
  
  <!-- Medium blur layer -->
  <text 
    x="640" 
    y="340" 
    class="vision-text"
    font-size="140"
    fill="rgba(255,255,255,0.3)"
    text-anchor="middle"
    filter="url(#blur-medium)">
    Vision
  </text>
  
  <!-- Subtle blur layer -->
  <text 
    x="640" 
    y="340" 
    class="vision-text"
    font-size="140"
    fill="rgba(255,255,255,0.5)"
    text-anchor="middle"
    filter="url(#blur-subtle)">
    Vision
  </text>
  
  <!-- Subtle tagline -->
  <text 
    x="640" 
    y="420" 
    class="vision-text"
    font-size="24"
    fill="rgba(255,255,255,0.4)"
    text-anchor="middle"
    letter-spacing="0.3em">
    MCP SERVER MANAGEMENT
  </text>
</svg>